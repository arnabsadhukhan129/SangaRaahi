const AppConfig = require('./app.config');
const EnumConfig = require('./enum.config');
const MailConfig = require('./mail.config');
module.exports = {
    AppConfig,
    EnumConfig,
    MailConfig
};

export enum MemberPromotionType {
  Promotion,
  Demotion
}
export enum PromotionStatus {
  Pending,
  Approved,
  Rejected,
  Completed
}
export enum CommunityType {
  Social,
  Cultural,
  Religious,
  Others
}
export enum RolesEnum {
  board_member,
  executive_member,
  member,
  fan,
  user
}

export enum DialogAction {
  DELETE,
  STATUS,
}

export enum DialogActionAdd {
  AddSMS,
  AddEmail,
  Edit,
  AddAdminSms,
  AddAdminEmail
}

export enum AlertDialogAction {
  ALERT
}

export enum DisplayPdfAction {
  VIEW
}
